<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <title>{% block title %}{% endblock %} — Legendary Palm Tree</title>
    {% block stylesheets %}
        {{ vite_entry_link_tags('theme')|replace({"<link": "<link crossorigin " }) | raw }}
    {% endblock %}
</head>
<body>
    <header class="tw-border-b tw-border-black tw-border-dashed tw-py-8 tw-text-center">
        <a href="{{ path('app_homepage')}}">Legendary Palm Tree</a>

        <p class="tw-mt-4">
            This repository contains all my testing for various web technologies that I encounter. I am using Symfony, Vue.JS and TailwindCSS as the general backbone for this site.
        </p>
    </header>
    {% block body %}{% endblock %}

    <footer class="tw-border-t tw-border-black tw-border-dashed tw-py-8 tw-text-center">
        <a href="{{ path('app_homepage')}}">Legendary Palm Tree</a>
        <div>Copyright © {{ 'now' | date('Y') }} <a target="_blank" rel="noopener" href="https://iusiel.netlify.app">Iusiel</a></div>
        
    </footer>

    {% block javascripts %}
        {{ vite_entry_script_tags('theme')|replace({"<script": "<script nonce=\"" ~ nonce.generate ~ "\"" }) | raw }}
        {{ vite_entry_script_tags("app")|replace({"<script": "<script nonce=\"" ~ nonce.generate ~ "\"" }) | raw }}
    {% endblock %}
</body>
</html>


